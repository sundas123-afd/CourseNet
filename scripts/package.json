{
  "name": "delete-stripe-data",
  "version": "1.0.0",
  "description": "Delete all Stripe data",
  "scripts": {
    "delete-stripe": "npx ts-node scripts/delete-stripe-data.ts"
  },
  "dependencies": {
    "stripe": "^16.12.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0"
  }
}
